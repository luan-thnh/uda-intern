/**
 * - Luon dung `const` neu khong thay doi gia tri
 * - Neu thay doi gia tri dung `let`
 * - Dat hang so bang `const`
 *
 * - Danh sach (so nhiu): them hau to `s/List`
 * - Kiem tra trang thai: tiem tien to `is/has/show/hide`
 * - Ham (func): bat dau la `dong tu (v)`: addStudent, covertToUppercase
 */
